
int main(int argc, char *argv[]) {
    // parse cmd args
    //CmdArgs Args = ParseArgs();
    // create the universe
    //Universe universe = Universe();  
    // add rules, initial alive cells

    /*
    if (Args.GetMode == ONLINE) {
        ;
    }
    else if (Args.GetMode == OFFLINE) {
        ;
    }
    */

    return 0;
}